type Token @entity {
  id: ID!
  roomId: BigInt
  provider: Bytes
  renter: Bytes
  approved: Boolean
  tokenId: BigInt!
  owner: Bytes # address
  creator: Bytes # address
  price: BigInt
  version: BigInt
  isPrepaid: Boolean
  isPaidAll: Boolean
}